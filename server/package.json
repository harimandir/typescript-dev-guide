{
  "name": "typescript-dev-guide-server",
  "version": "1.0.0",
  "description": "Express server with TypeScript",
  "scripts": {
    "start:build": "npx tsc -w",
    "start:run": "nodemon build/index.js",
    "start": "npx concurrently npm:start:*"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6",
    "typescript": "^4.0.5"
  }
}
