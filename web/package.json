{
  "name": "typescript-dev-guide-web",
  "version": "1.0.0",
  "description": "Homegrown web framework",
  "scripts": {
    "start:db": "npx json-server -w db.json",
    "start:parcel": "npx parcel index.html",
    "start": "npx concurrently npm:start:*"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "parcel-bundler": "^1.12.4",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "axios": "^0.21.0",
    "json-server": "^0.16.2"
  }
}
