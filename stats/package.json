{
  "name": "typescript-dev-guide-stats",
  "version": "1.0.0",
  "description": "Load, parse, analyze, and report CSV data",
  "scripts": {
    "start:build": "npx tsc -w",
    "start:run": "npx nodemon build/index.js",
    "start": "npx concurrently npm:start:*"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.5"
  }
}
